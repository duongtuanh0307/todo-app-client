import { style } from "@vanilla-extract/css";

export const inputUsernameField = style({
  display: "flex",
  flexDirection: "column",
  width: "100%",
});

export const actionButtons = style({
  display: "flex",
  justifyContent: "flex-end",
  marginTop: 10,
});
